apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-demo
  namespace: app-demo
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: app-demo
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
